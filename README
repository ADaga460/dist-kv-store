# Distributed KV Store

A simple distributed key-value store implemented in C++ with a server-client architecture using Winsock2. Supports SET, GET, and DUMP commands for managing key-value pairs over TCP/IP.